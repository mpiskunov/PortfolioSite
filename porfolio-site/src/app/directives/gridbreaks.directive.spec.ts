import { GridbreaksDirective } from './gridbreaks.directive';

describe('GridbreaksDirective', () => {
  it('should create an instance', () => {
    // const directive = new GridbreaksDirective();
    // expect(directive).toBeTruthy();
  });
});
